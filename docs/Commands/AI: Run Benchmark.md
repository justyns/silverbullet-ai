This command navigates to a benchmark page that runs simple tests against all configured models.

The tests check for proper tool support and usage.

The results are displayed on the benchmark page with detailed metrics for each model.  Multiple models can be tested in one go.
