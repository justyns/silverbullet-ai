{
  "name": "mcp-test-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal hello-world MCP server (Streamable HTTP) used to test the silverbullet-ai MCP client. Isolated from the plug bundle.",
  "scripts": {
    "start": "tsx server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
